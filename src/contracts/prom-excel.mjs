export const PROM_SHEETS = Object.freeze({
  PRODUCTS: 'Export Products Sheet',
  GROUPS: 'Export Groups Sheet',
});

export function headerIndex(headers, header) {
  return headers.indexOf(header);
}
